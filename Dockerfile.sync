FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir requests psycopg2-binary

# Copy sync script
COPY wiz-sync.py .

# Run the sync
CMD ["python", "wiz-sync.py"]